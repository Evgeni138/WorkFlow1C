# ext/ — выгрузка расширений конфигурации (CFE)

Сюда выгружаются расширения (каждое в своей подпапке), см. AGENTS.md «Структура».
После выгрузки — RLM-индексация (`rlm_index update`) для полного анализа.